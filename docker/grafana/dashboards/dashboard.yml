# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'HF Eco2AI Dashboards'
    orgId: 1
    folder: 'Carbon Tracking'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json